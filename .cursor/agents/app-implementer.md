---
name: app-implementer
description: >-
  Implements Murmur changes under app code paths. Apply @app-implementer
  skill + work folder skill. Phase E tasks for this area.
model: cursor-grok-4.5-high-fast
readonly: false
---

## Skills (load first)

1. **`@app-implementer`** — `.cursor/skills/subagents/app-implementer/SKILL.md`
2. **`@murmur-implementation`**
3. Work folder skill + plan wave task

**Scope:** `services/**`, root `*.py`, `tests/**`, `scripts/**`, `assets/**`, `Murmur.spec`, `requirements.txt`, `entitlements.plist` only. TDD. Minimal diff. Report files + tests.
